# Project Context — easypunto_parkos

> **Source of truth** for the SDD project context.
> Generated by `sdd-init` on 2026-08-30. Lean YAML lives in `openspec/config.yaml`;
> this file holds the full prose.

## Project Identity

- **Name**: easypunto_parkos
- **Working directory**: `E:\easypunto_parkos` (Windows; PowerShell 5.1)
- **Git repo**: NO (not yet initialized)
- **Obsidian vault**: `C:\Users\mccra\Documents\Obsidian\second_brain\` (single source of truth for personal notes)

## Current State

- **Files**: 1 — `modelo_datos_er.mmd` (1451 lines, 57 KB, Mermaid ER diagram)
- **Source code**: none
- **Package manifests**: none (no `package.json`, `pyproject.toml`, `go.mod`, etc.)
- **Config files**: none (no `tsconfig.json`, `vite.config.ts`, `.env.example`)
- **`.atl/`**: created by sdd-init
- **`openspec/`**: created by sdd-init
- **`.codegraph/`**: not present (CodeGraph unavailable for this project until `codegraph init` runs)

## Detected Stack

**Status**: empty project — stack pending design phase.

The user has communicated an **assumed stack** in the orchestrator preflight. This is a hypothesis, NOT a detected reality. The sdd-design phase must confirm or revise.

| Layer | Assumed (pending confirmation) |
|-------|--------------------------------|
| Backend | Python 3.13 + FastAPI + SQLAlchemy 2.0 (async) + Pydantic v2 + Alembic + uv |
| Frontend | React 18 PWA + Vite 5 + TypeScript strict + shadcn/ui + Zustand + react-hook-form + Zod + i18next |
| BFFs | 4 Node BFFs (one per PWA surface: Cajero, Kiosko, Admin-Op, Admin-Cfg) |
| Database | PostgreSQL 16+ (cloud admin + per-branch local) |
| Containerization | Docker + Docker Compose v2 |
| Migrations | Alembic (multi-schema, `prod.*`, monthly partitioning via `pg_partman`) |
| Tests (planned) | pytest + pytest-asyncio + factory-boy + testcontainers (backend); Vitest + RTL + axe-core (frontend) |

## Domain Context — Parking-Lot Management System

### Architecture

- **Multi-tenant cloud-edge**: cloud admin has a central Postgres; **each parking-lot branch has its OWN Postgres local**.
- **Bidirectional sync** between cloud and branches:
  - `sync_queue` — outbound operations queued for upstream propagation
  - `sync_log` — append-only log of sync attempts
  - `sync_conflict` — detected conflicts pending resolution
- 4 PWA surfaces + 4 Node BFFs:
  1. **Cajero** — cashier workstation
  2. **Kiosko** — self-service kiosk (fullscreen PWA, Wake Lock)
  3. **Admin-Op** — operational admin
  4. **Admin-Cfg** — configurator / white-label admin

### Data Model — AUDIT-FIRST (49 tables)

Three enforcement levels encoded in the Mermaid model:

| Marker | Count | Semantics | Enforcement |
|--------|-------|-----------|-------------|
| `[V]` versioned projection | 26 | Each change generates a new row (`vigente_desde`, `vigente_hasta`). Current state view. NOT source of truth. | UPDATE creates new version. DELETE never. |
| `[L]` lifecycle event | 11 | Three subtypes (see below) | Per subtype |
| `[L-E]` event puro | 3 | State is DERIVED from other registered events. NO mutation. (facturas, factura_electronica, ingreso) | INSERT-only. |
| `[L-W]` workflow | 6 | State transitions as NEW rows with FK `uuid_padre` to the original (chain). Last row of the chain = current state. (anulaciones, reclamos, alerta, reimpresion_ticket, envio_dian, validacion_evento) | INSERT-only. UPDATE/DELETE forbidden. |
| `[L-S]` session/cycle | 2 | UPDATE legitimate on `estado`/`fecha_cierre` with OBLIGATORY `log_transaccional` row in the same TX. (login, sesion) | UPDATE only on `estado` and `fecha_cierre`. |
| `[A]` source-of-truth | 12 | Inmutable enforced. Append-only. | REVOKE UPDATE, DELETE FROM `rol_app` ON `[A]_tables` (except `sync_queue`). Triggers RAISE EXCEPTION. |
| **Total** | **49** | | |

### Compliance — DIAN (Colombia)

- Electronic invoicing (`factura_electronica`, `revocacion_factura`)
- **Retention 5+ years** (DIAN mandate) — `fecha_retencion_hasta` column
- **SHA256 hash chain** on `log_transaccional` and `revocacion_factura` for probatory integrity (`hash_anterior` → `hash_actual`)
- Probatory evidence chain for tax authority

### Common Audit Columns

- `timestamp_evento` — when the business event happened (distinct from `created_at`)
- `fecha_retencion_hasta` — retention deadline per row
- `hash_anterior` / `hash_actual` — integrity chain on critical logs
- `uuid_padre` — self-FK for `[L-W]` workflow chains

## Session Preflight Decisions (cached)

| Decision | Value |
|----------|-------|
| Artifact store mode | `hybrid` (Engram + openspec/) |
| Pace | `interactive` |
| Delivery strategy | `ask-on-risk` |
| Review budget | `400 lines` per change |
| User conversation language | Spanish (rioplatense / voseo) |
| Technical artifact language | English (default per sdd-init Language Domain Contract) |

## Next Step — Recommended

**Concrete recommendation**: start with `/sdd-new bootstrap-monorepo-foundation` (or any change name you prefer — the orchestrator's preflight will prompt). The first change should:

1. Crystallize the **monorepo skeleton** (`backend/`, `apps/{cajero,kiosko,admin-op,admin-cfg}/`, `bff/{cajero,kiosko,admin-op,admin-cfg}/`, `workers/`, `infra/`)
2. Confirm the **assumed stack** from this init (or revise it) — the sdd-design phase is the gate, NOT this init.
3. Establish **DIAN audit-first** as a non-negotiable constraint for every subsequent change (the model is the source of truth; do not drift from the 3-level enforcement).

**Alternative first move**: `/sdd-explore cloud-edge-sync-architecture` if you want to pressure-test the sync-queue conflict-resolution flow before committing to scaffolding.

## Risks and Caveats

- **Empty project + assumed stack** — the assumed stack is unconfirmed; sdd-design must ratify or revise.
- **No git repo yet** — OpenSpec file benefits (versioning, audit trail) are dormant until `git init` runs.
- **No test runner** — strict TDD is off. Re-evaluate after first backend `pyproject.toml` or first `package.json` lands.
- **No `.codegraph/`** — structural queries fall back to filesystem tools until `codegraph init` is run.
- **49-table AUDIT-FIRST model is rich** — first scaffold risk: choosing a code structure that can't honor all three audit levels. Recommend sdd-design produce a backend module layout (`models/` partitioned by audit level) BEFORE first migration.
- **DIAN compliance is hard** — retention 5+ years + SHA256 hash chain + REVOKE on `[A]` tables is not optional. The model `.mmd` MUST be the source of truth; do NOT regenerate from prose.
- **Multi-tenant + cloud-edge** — sync conflicts will happen. Plan for `sync_conflict` resolution strategy in sdd-design.
