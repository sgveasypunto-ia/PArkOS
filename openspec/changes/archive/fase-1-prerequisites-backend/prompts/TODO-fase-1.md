# TODO Fase 1 — easypunto_parkos (Parte I)

> Tracking de las 15 HU de Fase 1 sobre la rama `feat/fase-1-prerequisites-backend`.
> Cada HU cierra con: commit atómico, reporte (`HU-F1.X-report.md`), observación Engram.
> Marca de estado: `[ ]` pendiente · `[~]` en curso · `[x]` cerrada · `[!]` bloqueada.

## Estado actual

- **Rama:** `feat/fase-1-prerequisites-backend`
- **Base:** `origin/dev` + merge de `feature/motor_sync_correcciones_integridad` (22 commits) + 1 commit pre-Fase-1 (bcrypt real en login)
- **Commits en rama antes de Fase 1:** 23 (1 merge + 22 absorbidos + 1 pre-Fase-1)
- **PR target:** contra `origin/dev` (gitflow)

## HUs (15)

| # | ID | Título | Tamaño est. | Estado | Commit | Reporte | Notas |
|---|---|---|---|---|---|---|---|
| 1 | HU-F1.1 | router_factory.py: orden sin `vigente_desde` para workflows | 60 LOC | `[x]` | `f7cb37a` | `HU-F1.1-report.md` (a generar al cerrar Fase 1) | GAP-BE-02 + cascada a Caja/Arqueo/Sesion |
| 2 | HU-F1.2 | GET /auth/me, cookie httpOnly, lockout real | 210 LOC | `[x]` | `535676d` | `artifacts/HU-F1.2-report.md` | commit: feat(backend): cerrar HU-F1.2 — cookie parkos_session + lockout real + GET /auth/me. 21 tests, 0 CRITICAL |
| 3 | HU-F1.3 | Constraint sesión única + GET /caja-sesion/sesion/me | 110 LOC | `[x]` | `4d530a4` (docs) + `ca3f9bf` (código) + `467b4f0` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-3-sesion-unica/archive-report.md` | migration 0023 (post-0022 ya consumido por F1.8). Defense in depth completa: BD partial unique index `prod.uq_prod_sesion_one_active_per_user` con `CONCURRENTLY` + pre-flight `DO $$`; repo `session_cycle.open_session` captura `IntegrityError` por pgcode `23505` → re-emite `SesionAlreadyActive`; handler `open_sesion` mapea 409 `{"error":"sesion_already_active"}` (pgcode nunca expuesto); handler `get_my_sesion` registrado ANTES del `include_router(make_router(...))` con `_sesion_issuer_dep = requires_issuer("operador-", "admin-")` y 404 `sesion_no_active`. Helper puro `repo/sesion_activa.get_sesion_activa` (+48 LOC) reusable. REQ-OPS-026..029 merged en `openspec/specs/operations/spec.md`. verify-report PASS (0 CRITICAL/HIGH/MEDIUM, 2 LOW pre-existing housekeeping). 7/7 tests GREEN. Siguiente sugerida: F1.5 (160 LOC, no bloqueada por KD-IVA). |
| 4 | HU-F1.4 | Filtro vigente_en en tarifas_sucursal | 60 LOC | `[x]` | `de4d2fc` (código) + `3844524` (docs) + `22b8db6` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-4-tarifas-vigente/archive-report.md` | solo backend, openspec canónico, REQ-OPS-017..021 mergeados en `openspec/specs/operations/spec.md`. verify-report PASS (0 CRITICAL/HIGH/MEDIUM).
| 5 | HU-F1.5 | mv_ocupacion_diaria + GET /operacion/ocupacion | 160 LOC | `[x]` | `bb99e18` (código) + `fc72adb` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-5-mv-ocupacion-diaria/archive-report.md` | migration 0024 (post-0023 ya consumido por F1.3) — primer `CREATE MATERIALIZED VIEW` del schema. Defense in depth completa: pre-flight `DO $$` 10M NOTICE / 50M ABORT (KD-7) + `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS prod.uq_mv_ocupacion_diaria_sucursal_tipo` (KD-2) + repo SQL `tipos_vehiculo` LEFT JOIN MV (D-F1.5-1 KD-6-respecting, F4.3 strip empty-state UX) + `RefreshMvOcupacionWorker(WorkerRunner)` con KD-5 fallback `REFRESH MATERIALIZED VIEW` plain + KD-3 chain `get_ocupacion` (400 `missing_sucursal_context` / 403 `tenant_scope_violation`) + `Cache-Control: no-store` + AST walk read-only. REQ-OPS-030..033 merged en `openspec/specs/operations/spec.md`. verify-report PASS (0 CRITICAL/HIGH/MEDIUM, 3 LOW pre-existing baseline). 11 tests GREEN (3 re-run this session via `--noconftest`, 8 SKIP por `postgres_container` fixture Docker not available). `worker_base_intact` + `factory_intact` + `__init__.py_intact` CI gates verified (empty diffs). Siguiente sugerida por plan.md línea 792: F1.6 (260 LOC, depende F1.5 + F1.4, KD-IVA no aplica). |
| 6 | HU-F1.6 | Validaciones reales en POST /operacion/ingresos | 260 LOC | `[x]` | `fff6350` (apply) + `8590e2b` (tests) + `ff41f8c` (fix M1+M2+L1) + `21097c8` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-6-validaciones-post-ingresos/archive-report.md` | migration 0025 (post-0024) — primer `ADD COLUMN JSONB datos_nuevos` + siembra `capacidad_agotada_forzado` (alert_types_inmutable trigger respetado). Defense in depth 4 capas (D-HU-F1.6-7): regex server-side override (V5) + KD-FORZADO-01 prefix `[FORZADO: <motivo ≥10 chars>]` (3 discriminadores tipados: `forzado_contradiccion`/`motivo_forzado_requerido`/`motivo_forzado_insuficiente`) + alerta `capacidad_agotada_forzado` same-TX solo cuando V2 fue bypassed (R2 mitigation) + AST walk `test_kd_forzado_in_handler.py` enforcing orden literal V5→V4→KD-FORZADO→V1+V2→V3→V6→V8→INSERT→alerta (R7 invariante). 9 validaciones V1..V9 server-side (regex estricta Colombia `[A-Z]{3}[0-9]{3}` Auto / `[A-Z]{3}[0-9]{2}[A-Z]` Moto, NO O↔0 tolerancia DEC-SUC-22); `tipo_entrada` derivado server-side `MENSUALIDAD if uuid_subscripcion_cliente else ROTACION` (DEC-SUC-21, NUNCA persistido); V8 EXISTS directo a `prod.ingreso` (NO MV, 10s lag RIESGO-SUC-02 aceptado). `repo/ingreso.py` (+194 LOC) single import surface con `__all__` (R-A5); `repo/placa.py` (+67 LOC) regex hardcoded; `repo/subscripcion_activa.py` (+149 LOC) reusa `bitemporal_vigente_predicate` F1.4 + lift verbatim de `resolve_active_subscription_for_exit` para F7 reuse; `repo/alerta.py` (+57 LOC) JSONB `datos_nuevos` audit; `repo/ocupacion.py` (+86 LOC) `validar_cupo_disponible`; `repo/tarifas_vigencia.py` (+49 LOC) `validar_tarifa_vigente`; `api/v1/operacion.py` `create_ingreso` (11-step chain, +226/-26 LOC); `schemas/operacion.py` (+116 LOC, 9 nuevas clases: IngresoCreateForzado + IngresoReadForzado + 7 typed errors con `Literal[]` discriminators + `extra='forbid'` heredado de `_Base`); `api/v1/_helpers.py` (+34 LOC, `no_store_headers` + `apply_no_store_header` R-A6). 4 commits. verify-report: 0 CRITICAL/HIGH, 3 MEDIUM deviations (M1 prefix `startswith`+`rstrip` MIGRATED from `in`+`endswith`; M2 EXISTS missing 3 filters MIGRATED adding `s.uuid_sucursal` + `a.estado='ejecutada'` + `a.tipo_anulable IN ('ingreso','salida')`; M3 spec delta merged at archive) + 1 LOW (L1 unused `forzado` kwarg removed). REQ-OPS-034..041 merged en `openspec/specs/operations/spec.md` (lines 902-1283). 9 test files / ~28 tests (8 HTTP unit + 6 KD-FORZADO/regex/mock unit + 3 DB integration + 3 migration + 2 AST walks). CI gates verified empty diff (factory_intact, event_helper_intact, auth_tenancy_intact, __init__.py_intact). Siguiente sugerida: F1.7 (220 LOC, depende F1.8 cerrado, KD-IVA inline-seed plan). |
| 7 | HU-F1.7 | POST /operacion/salidas (rotación + mensualidad) | 220 LOC | `[x]` | `c320d0f` (apply) + `aa2fc9b` (tests) + `f826e8c` (fix D0+D1+D8) + `b4f1b45` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-7-salidas/archive-report.md` | migration 0026 (post-0025) — `INSERT INTO prod.impuestos (..., 'IVA', 0.19, ...) ON CONFLICT (codigo, vigente_desde) DO NOTHING` (KD-IVA inline-seed F1.8 blocker RESUELTO inline DEC-IMP-01) + siembra 2 `alert_types` (`subscripcion_vencida_forzado` + `tarifa_vigente_forzado`, alert_types_inmutable trigger respetado) + `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS one_exit_per_ingreso ON prod.salidas (uuid_ingreso) WHERE NOT EXISTS (anulaciones ejecutadas)` (KD-S16 TOCTOU closure, R4). Defense in depth 5 capas (D-HU-F1.7-7): KD-3 issuer chain (`_ingreso_issuer_dep = requires_issuer("operador-", "admin-")` reuse) + KD-FORZADO-01 prefix `[FORZADO: <motivo ≥10 chars>]` (verbatim reuso F1.6 `validar_kd_forzado`, 3 discriminadores tipados, DEC-FORZADO-01) + alerta same-TX solo cuando V2 (`subscripcion_vencida_forzado`) o V5 (`tarifa_vigente_forzado`) fue bypassed (R2 mitigation, 1 alerta per bypass) + AST walk `test_salida_handler_step_order.py` enforcing orden literal 12-step V1→tenant_scope→V2→V3→V4→V5→INSERT→alerta→single_commit→derivar_tipo→response (KD-S7 lock continuity en `tarifas_sucursal` via F1.8 PL/pgSQL VOLATILE `SELECT … FOR SHARE` invariante) + partial unique index (R4) + REVOKE/IMMUTABLE baseline. 5 validaciones V1..V5 server-side: V1 `404 ingreso_no_encontrado` unified discriminator (no existe / ya cerrado / anulado, no bypass); V2 `422 subscripcion_inactiva_o_vencida` con bypass `subscripcion_vencida_forzado`; V3 `422 placa_no_coincide_con_ingreso` (placa opcional KD-S3, no bypass); V4 KD-FORZADO-01 verbatim reuso F1.6; V5 `422 tarifa_vigente_no_encontrada` con bypass `tarifa_vigente_forzado` + `500 iva_no_configurado` (post-0026 deploy: never). `tipo_salida` derivado server-side `MENSUALIDAD if cotizar.cobrar=false else ROTACION` (DEC-SUC-21-NEW, NUNCA persistido en `prod.salidas`, by 4FN design); `cotizacion_snapshot` populated for ROTACION, null for MENSUALIDAD (KD-S4). `models/A/salidas.py::Salidas(AppendOnlyBase)` REUSADO pre-existente PR1a (composite PK `uuid+fecha_retencion_hasta`, monthly `RANGE (fecha_retencion_hasta)` partition, `__write_only__` marker habilita AST-level DML rejection via `tests/static/test_no_raw_dml_on_a_tables.py` — SUPERIOR a `LifecycleEventBase` propuesto originalmente, D-HU-F1.7-14 supersession); `repo/salida.py` (+~180 LOC, 4 helpers + 1 typed exception `SalidaDuplicada`); `repo/impuestos.py` (+~50 LOC, `validar_iva_configurado`); `api/v1/operacion.py` `create_salida` (12-step chain, +~210 LOC); `schemas/operacion.py` (+~95 LOC, `SalidaCreateForzado` + `SalidaReadForzado` + 4 typed errors con `Literal[]` discriminators + `extra='forbid'` heredado). 4 commits. verify-report: 0 CRITICAL/HIGH, 1 MEDIUM (D0 DB integration tests T5.3+T5.4 not shipped) + 3 LOW (D1 design prose drift on Salida ORM resolved at archive via design.md §3+§9.1 amendment; D2 same-DEC schema naming collision acceptable; D8 stale `impuestos_inmutable` docstring). F1.7 fix commit `f826e8c` resolved D0 (added `test_salida_create_db.py` 489 LOC 3 tests + `test_migration_0026_idempotent.py` 216 LOC 5 tests mirroring F1.6 pattern with `pg_engine`+`alembic_upgrade` gate), D1-cont (2 schemas docstring fixes `models/L_S/salida.py` → `models/A/salidas.py` L386+L410 + `AppendOnlyBase` L402), and D8 (migration 0026 L222-225 stale trigger reference replaced with verified-fact language). REQ-OPS-042..052 merged en `openspec/specs/operations/spec.md` (lines 1377-2197, 11 new requirements, ~44 KB verbatim). 8 test files / ~13 new tests (10 HTTP unit + 2 KD-FORZADO aux unit + 3 DB integration + 1 migration idempotency + 2 AST walks). CI gates verified empty diff (`__init__.py_intact`, `factory_intact`, `event_helper_intact`). KD-IVA blocker RESUELTO inline (MIGRATION 0026 Op 2); F1.8 ya no bloqueada por IVA configuration; siguiente sugerida: HU-F1.9 (Facturación transaccional + NIT módulo 11, 6 HUs restantes). |
| 8 | HU-F1.8 | calcular_cotizacion + GET /operacion/cotizar | 200 LOC | `[x]` | `9ebaed6` (docs) + `a3d0c39` (código) + `7682a57` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-8-cotizar/archive-report.md` | PL/pgSQL VOLATILE (Postgres rechaza FOR SHARE en STABLE, AST walk preserva read-only). REQ-OPS-022..025 merged en openspec/specs/operations/spec.md. REQ-OPS-025 reconciliado. KD-IVA deployment blocker (impuestos.IVA siembra, ownership HU-F14.2 Parte II). verify-report PASS (0 CRITICAL/HIGH/MEDIUM, 2 LOW). 7/7 tests GREEN. |
| 9 | HU-F1.9 | Facturación transaccional + NIT módulo 11 | 330 LOC | `[x]` | `6d2c457` + `6afc923` + `a2a43bc` + `f9d41e6` + `610c5a0` + `33681d8` + `898cdae` + `e2f39ae` + `91aff26` (fix) + `5c4dd31` (cosmetic) + `f00c848` (archive) | `openspec/changes/archive/2026-09-14-hu-f1-9-facturacion/archive-report.md` | migration 0027 (post-0026) — `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS one_factura_per_salida ON prod.facturas (uuid_salida) WHERE uuid_salida IS NOT NULL` (KD-S16 TOCTOU closure, R4) + `CREATE OR REPLACE FUNCTION prod.fn_factura_pagos_init_pago_uniqueness()` BEFORE INSERT trigger on partitioned `prod.factura_pagos` (analogía migration 0004 L35-66). Defense in depth 5 capas: KD-3 issuer chain (`_facturacion_issuer_dep = requires_issuer("operador-", "admin-")`) + KD-FACT-02 SELECT FOR SHARE per-row over `prod.tarifas_sucursal` (held until session.commit()) + partial unique index (R4) + BEFORE INSERT trigger (R3 partition infeasibility closure) + IntegrityError → typed exception → 409 mapping. NIT módulo 11 Variant A canónica per DIAN Resolución 000175 de 2021 (`dv = sum % 11`); reference test `800.123.456-7` → DV=7 discriminates from Variant B. `repo/nit_modulo11.py` (~50 LOC) wired into Pydantic v2 `FacturaItemConDatosPropios` `@field_validator("numero_identificacion")` + `ClientesCreate` reuse. `repo/factura.py` (~250 LOC, 9 helpers + 5 typed exceptions) + `repo/factura_detalle.py` (~30 LOC bulk insert) + `repo/impuestos.py` extended with `obtener_iva_vigente` helper (DEC-FACT-03 IVA consistency). `api/v1/facturacion.py` `create_factura` 12-step chain (KD-3 → V1 salida → tenant scope → V2 cliente → V3 IVA → V4 detalle → KD-FACT-02 FOR SHARE → V6 total → INSERT facturas → INSERT detalle + impuestos + pagos → single `await session.commit()` → response with `Cache-Control: no-store`). `create_factura_pago` 5-step chain (KD-3 → V5 voucher_requerido → tenant scope → INSERT factura_pagos → single commit). 11 REQs (REQ-OPS-053..063) + 3 cross-cutting (REQ-OPS-XR1..XR3) merged en `openspec/specs/operations/spec.md` (canonical now 66 total). verify-report PASS post-fix (`91aff26` resolved C1 AttributeError on item.uuid, C2 placeholder buscar_salida_facturable, C3 hardcoded iva=0.19). 53 tests PASS (47 original + 6 new TDD tests). 4 deviations (D1 issuer guard cajero-→operador-, D2 migration filename, D3 frontend stub location, M1 DB-coupled tests deferred to CI via PARKOS_DOCKER_TEST=1). F1.9 closes billing half of CU-04. Siguiente sugerida: HU-F1.10 (Numeración FE + estado DIAN + reintento, 230 LOC). |
| 10 | HU-F1.10 | Numeración FE + estado DIAN + reintento | 230 LOC | `[ ]` | — | — | assign_consecutivo ya existe |
| 11 | HU-F1.11 | Workflow reimpresión tiquete (crear + anular) | 170 LOC | `[ ]` | — | — | gap huérfano: anulación |
| 12 | HU-F1.12 | Venta atómica de suscripción | 260 LOC | `[ ]` | — | — | ampliación producto, no CU literal |
| 13 | HU-F1.13 | Endpoints arqueo + siembra cierre_dia | 240 LOC | `[ ]` | — | — | GAP-BE-05 (permiso mal) bundleado acá |
| 14 | HU-F1.14 | GET /sync/estado + 11 alert_types nuevos | 120 LOC | `[ ]` | — | — | 8 técnicos ya existen |
| 15 | HU-F1.15 | GET /usuarios/{uuid}/login histórico | 70 LOC | `[ ]` | — | — | gap huérfano |

## Notas operativas

- **Branch:** pushear al final de cada HU (`git push origin feat/fase-1-prerequisites-backend`).
- **Cada HU:** un sub-agente fresco, fresh context, summary de 5-8 líneas + 5 checks `[OK]`.
- **Verificación externa:** el orquestador valida cada entrega con los 5 checks del prompt §4.3 antes de pushear.
- **Gaps transversales absorbidos en la rama:**
  - GAP-BE-02 (router_factory) → lo cierra HU-F1.1 (mejora además Caja/Arqueo/Sesion por el fix genérico).
  - GAP-BE-05 (permiso mal asignado) → bundleado en HU-F1.13.
  - GAP-BE-06 (rol parkos_app) → ya cerrado en el merge de `feature/motor_sync_correcciones_integridad` (commit `5edb317`).
- **Conventional Commits:** mensajes en español neutral, sin `Co-authored-by:` ni AI trailers.
- **Tope por commit:** <800 LOC (regla `config.yaml rules.tasks`).

## Riesgos vivos a monitorear

- `router_factory.py` ya tenía fix parcial (commit `dd500c2`) para binding PEP 563, pero el bug de `vigente_desde` sin guarda sigue presente (lo cierra HU-F1.1).
- `costos_servicios` necesita un row con concepto='reimpresion' sembrado para HU-F1.11 — verificar al implementar.
- `configuracion_seguridad` no tiene row default global sembrado — verificar al implementar HU-F1.2.
- `impuestos.IVA` necesita estar sembrado para HU-F1.8 (`iva_no_configurado`).
- `tipo_arqueo.cierre_dia` requiere siembra explícita (HU-F1.13).
- `alert_types` ya tiene 8 sembrados (técnicos); HU-F1.14 añade 11 (negocio) — total 19, idempotente.
